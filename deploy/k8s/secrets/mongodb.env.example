# Copie para mongodb.env (gitignored) ou gere com `make k8s-secrets`.
# A senha aparece duas vezes: em `password` (para o mongod) e dentro de `uri`
# (para o serviço Movies) — mantenha as duas iguais.
username=movies
password=CHANGE_ME
uri=mongodb://movies:CHANGE_ME@mongodb:27017/?authSource=admin
