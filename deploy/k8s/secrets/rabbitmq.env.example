# Copie para rabbitmq.env (gitignored) ou gere com `make k8s-secrets`.
# A senha aparece duas vezes: em `password` (para o broker) e dentro de `url`
# (para o serviço Movies) — mantenha as duas iguais.
username=movies
password=CHANGE_ME
url=amqp://movies:CHANGE_ME@rabbitmq:5672/
